[project]
name = "recipe-app-dev"
version = "0.1.0"
description = "Development server for Recipe App"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
]

[tool.uv]
dev-dependencies = []